# MyApp

Generated application

**Version:** 1.0.0  
**Author:** ROQUE GARCIA MARTINEZ   
**Company:** Menchi Boutique

Generated by [ExeForge](https://exeforge.app) — HTML to Windows EXE Builder.

## Prerequisites

- Windows 10 or 11
- Node.js 18 or newer — https://nodejs.org
- Git — https://git-scm.com
- Python 3 — https://python.org
- Visual Studio Build Tools — https://visualstudio.microsoft.com/visual-cpp-build-tools/
- NSIS — https://nsis.sourceforge.io (required for installer builds)

## Quick start

1. Extract all files to a folder.
2. Run **build.bat** (or `npm install && npm run build`).
3. Find the output in the `dist/` folder.

## Project structure

```
MyApp/
  main.js              — Electron main process
  preload.js           — Secure preload bridge
  package.json         — App metadata and build config
  electron-builder.json — Electron Builder configuration
  installer.nsi        — NSIS installer script
  index.html           — Your application UI
  style.css            — Your styles
  script.js            — Your scripts
  build.bat            — Windows build helper
  build/icon.ico       — Application icon (place your icon here)
```

## Notes

- Use relative paths (`./assets/app.css`) — absolute paths break in packaged builds.
- No server-side code (PHP, Python, etc.) runs inside the app.
- Sign the output with a code signing certificate to avoid SmartScreen warnings.
- Bump "version" in package.json for every release.
